/**
 * Participant submit UI for the challenge pane (creation ID form removed).
 * @param {{ phase: string, viewerId: number | null }} _vm
 */
export function renderSubmitSection(_vm) {
	return '';
}
